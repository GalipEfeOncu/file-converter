# Issue #18 — Agent Çalışma Raporu

**Üye:** Aksar712 (Abdulkadir Sar)
**Tarih:** 2026-04-27
**Branch:** feat/issue-13-viewer-dispatcher
**Model:** Gemini 3.1 Pro (High)

## 1. Anladığım Görev
"Görüntüle" sekmesine benzer şekilde, "AI Analizi" sekmesinin UI bileşenlerini oluşturmak. Desteklenen dosyalardan (PDF, DOCX, TXT, CSV) `core/viewer.py` içerisindeki yeni `extract_text` metodu ile düz metin çıkarıp, Gemini API'ye göndererek Özetleme, Soru Sor, Anahtar Kelime Çıkarma ve Sadeleştirme işlemlerini `st.session_state` kullanarak arayüze bağlamak.

## 2. Plan (Kabul Kriterlerine Karşılık)
- [x] AC #1 → UI'da `.pdf`, `.docx`, `.txt`, `.csv` dışındaki dosyalarda `st.info` gösterimi sağlandı (`ui/dashboard.py`).
- [x] AC #2 → `core/viewer.py` içerisine PyMuPDF, python-docx ve yerleşik `open` kullanarak `extract_text` eklendi.
- [x] AC #3 → Özetle, Soru Sor (input ile) ve Anahtar Kelime Çıkar butonları eklendi. Ek olarak Sadeleştir özelliği bağlandı (`ui/dashboard.py`).
- [x] AC #4 → `summarize` butonu için `short, medium, long` uzunluk seçici `st.radio` eklendi (`ui/dashboard.py`).
- [x] AC #5 → Cevaplar `st.expander` içerisine `st.markdown` ile basıldı, kaybolmasını engellemek için `st.session_state`'e kaydedildi (`ui/dashboard.py`).
- [x] AC #6 → Tüm AIEngine çağrıları `st.spinner` ile sarıldı (`ui/dashboard.py`).
- [x] AC #7 → API hatası durumlarında veya dosya okunamadığında `st.error` / `st.warning` ile hata yönetimi yapıldı (`ui/dashboard.py`).

## 3. Değiştirilen / Eklenen Dosyalar
| Dosya | Tip | Satır (+/-) | Açıklama |
|-------|-----|-------------|----------|
| `core/viewer.py` | Modifiye | +25 | `extract_text` metodu eklendi |
| `ui/dashboard.py` | Modifiye | +72 | `tabs[2]` içerisi AI butonları ve `AIEngine` ile değiştirildi |
| `assets/languages.json` | Modifiye | +20 | AI butonları ve hata mesajları için TR ve EN anahtarları eklendi |
| `tests/test_viewer.py` | Modifiye | +22 | `extract_text` metodu için bir adet başarılı ve bir hata testi eklendi |

## 4. Atlanan / Yapılamayan Maddeler
Atlanan veya yapılamayan madde bulunmamaktadır. Tüm fonksiyonlar sorunsuz şekilde bağlandı.

## 5. Test Sonuçları
- Komut: `python -m pytest tests -v`
- Sonuç: PASS (16 passed)
- Yeni eklenen testler: 
  - `test_extract_text_from_txt`
  - `test_extract_text_unsupported_ext`

## 6. Dökümantasyonda Fark Ettiğim Sorunlar
Yok

## 7. Önerilen Commit Mesajı
```text
feat(ui): connect AI tab UI to AIEngine for Issue #18

- Add extract_text() to FileViewer for text extraction
- Build AI tab layout with Summarize, Ask, Keywords, Simplify
- Preserve AI results across interactions using st.session_state
- Add new i18n keys for AI actions

Refs: Issue #18
```

## 8. Koordinasyon / Delegasyon Notları
- **i18n key eklemeleri (Ali için):** `assets/languages.json` içerisine AI butonları için 8 adet yeni anahtar eklenmiştir. Dil dosyalarının son hali gözden geçirilebilir.
- **main.py değişikliği gerekli mi (Galip Efe için):** Hayır. İşlem `Dashboard` sınıfı içerisinde izole edilmiştir.


---

## ✅ Scrum Master İnceleme Notu
- **İnceleyen:** Scrum Master
- **Tarih:** 2026-04-29
- **Durum:** Rapor incelendi; ilgili görevler `docs/ROADMAP.md` ve `docs/AGENT_GUIDE.md` üzerinde güncellenmiştir.

