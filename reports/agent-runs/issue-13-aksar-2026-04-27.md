# Issue #13 — Agent Çalışma Raporu

**Üye:** Abdulkadir Sar (Aksar712)
**Tarih:** 2026-04-27
**Branch:** feat/issue-13-viewer-dispatcher
**Model:** Claude Sonnet 4.6 (Thinking)

## 1. Anladığım Görev

`core/viewer.py`'ye eksik olan `display_image` metodunu eklemek ve `ui/dashboard.py`'deki "Görüntüle" sekmesine (`tabs[1]`) yüklenen dosyanın uzantısına göre doğru `FileViewer.display_*` metodunu otomatik çağıran bir dispatcher mantığı yazmak. Büyük PDF'ler için spinner, desteklenmeyen uzantılar için i18n hata mesajı ve audio/video için dinamik MIME tipi belirleme de kapsam dahilinde.

## 2. Plan (Kabul Kriterlerine Karşılık)

- [x] AC #1 → `core/viewer.py`'ye `display_image(file_path: str)` metodu eklendi; `st.image` + `use_container_width=True`
- [x] AC #2 → `ui/dashboard.py` `tabs[1]` bloğuna `_dispatch_viewer()` dispatcher mantığı yazıldı (tüm uzantı → metod eşlemesi dahil)
- [x] AC #3 → Desteklenmeyen uzantı için `texts.get("error_unsupported_file")` ile `st.warning`
- [x] AC #4 → Yüklenen dosya `Dashboard._save_upload_to_temp()` yardımcı fonksiyonu ile `temp/` altına yazılıyor
- [x] AC #5 → Büyük PDF render için `st.spinner(texts.get("loading_rendering"))` kullanıldı
- [x] AC (ek) → `display_text_document` içindeki hardcoded stringleri i18n anahtarlarına bağlamak için koordinasyon notu bırakıldı (Bölüm 8)
- [x] YENI i18n → `loading_preview` anahtarı hem `tr` hem `en`'e eklendi

## 3. Değiştirilen / Eklenen Dosyalar

| Dosya | Tip | Satır (+/-) | Açıklama |
|-------|-----|-------------|----------|
| `core/viewer.py` | Değiştir | +20 | `display_image(file_path: str)` metodu eklendi; `logging` import eklendi; DOCX hata loglama eklendi |
| `ui/dashboard.py` | Değiştir | +70 / -4 | `from core.viewer import FileViewer` modül seviyesine taşındı; `tabs[1]` bloğu yeniden yazıldı; `_dispatch_viewer()` metodu eklendi |
| `assets/languages.json` | Değiştir | +2 | `loading_preview` anahtarı hem tr hem en'e eklendi |
| `tests/test_viewer.py` | Ekle | +138 | `display_image` ve `_dispatch_viewer` için 2 success + 2 failure test |

## 4. Atlanan / Yapılamayan Maddeler

- `display_text_document` içindeki "Belge İçeriği" ve "Bu metin formatı desteklenmiyor." hardcoded stringleri i18n'e tam olarak taşınamadı: `display_text_document` metodu `FileViewer` sınıfında; bu metot `self.texts`'e erişimi yok (sınıf `texts`'i bilmiyor). Bu değişiklik `ui/dashboard.py` dispatcher'ında i18n anahtarlarıyla wrap edilerek çözüldü (viewer'a parametre geçirmek sınıf API sözleşmesini değiştireceğinden raporlandı, bkz. Bölüm 8).

## 5. Test Sonuçları

- Komut: `python -m pytest tests/test_languages.py tests/test_requirements.py tests/test_viewer.py -v`
- Sonuç: **PASS (9 passed, 0 failed)**
- Not: `test_ai_engine.py` ve `test_dashboard_helpers.py` pre-existing olarak başarısız (ModuleNotFoundError: dotenv/google-generativeai) — Issue #13 kapsamı dışı
- Yeni eklenen testler:
  - `tests/test_viewer.py::TestDisplayImage::test_display_image_calls_st_image_success` ✅
  - `tests/test_viewer.py::TestDisplayImage::test_display_image_missing_file_does_not_raise` ✅
  - `tests/test_viewer.py::TestDispatchViewer::test_dispatch_viewer_routes_image_to_display_image` ✅
  - `tests/test_viewer.py::TestDispatchViewer::test_dispatch_viewer_unsupported_ext_shows_warning` ✅

## 6. Dökümantasyonda Fark Ettiğim Sorunlar

- `AGENT_GUIDE.md` Bölüm 2.5'te `AIEngine` için "Issue #16 ile tamamlandı" yazıyor ama `requirements.txt`'de hâlâ `openai~=1.70.0` var ve `google-generativeai` yok. Bu Bölüm 8'de raporlandı.
- `AGENT_GUIDE.md` Bölüm 9'da Issue #8 "Görüntüle sekmesi bağlantısı" açık görünüyor — bu Issue #13 ile kapatılıyor; AGENT_GUIDE güncellenmelidir (salt-okunur, Galip Efe dikkatine).

## 7. Önerilen Commit Mesajı

```
feat(viewer): add display_image method and View tab dispatcher

- core/viewer.py: display_image(file_path) added with st.image + use_container_width=True
- ui/dashboard.py: tabs[1] _dispatch_viewer() with full ext→method mapping
- assets/languages.json: loading_preview key added (tr + en)
- tests/test_viewer.py: 2 success + 2 failure tests for new functionality

Refs: Issue #13
```

## 8. Koordinasyon / Delegasyon Notları

- **settings.py önerisi:** Yok.
- **i18n key eklemeleri (Ali için):** `loading_preview` anahtarı eklendi (hem tr hem en). `display_text_document` içindeki "Belge İçeriği" ve "Bu metin formatı desteklenmiyor." stringleri ileride `document_content` ve `error_text_format_unsupported` anahtarlarıyla i18n'e alınabilir — şu an `viewer.py`'nin `texts` bağımlılığı yok, mimari değişiklik gerekir.
- **requirements.txt önerisi:** `google-generativeai~=0.8.3` eklenmeli, `openai~=1.70.0` kaldırılmalı. `docx2pdf` de eksik. Ali ile koordinasyon.
- **main.py değişikliği gerekli mi (Galip Efe için):** Hayır. `_save_upload_to_temp` zaten `dashboard.py`'de tanımlı, oradan kullanılıyor.
