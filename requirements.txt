# --- Arayüz ---
streamlit            # Web tabanlı uygulama arayüzü 

# --- Belge ve Tablo İşlemleri ---
pandas             # CSV ve Excel işlemleri
openpyxl           # Excel dosyalarını yazmak için gerekli
python-docx        # Word (.docx) dosyalarını okuma/yazma
PyPDF2             # PDF metin çıkarma ve basit işlemler
PyMuPDF            # PDF'leri resme çevirme (Görüntüleyici için fitz)
Pillow             # Resim işleme (PNG/JPG/WEBP dönüşümleri)

# --- Ses ve Medya ---
pydub              # Ses formatı dönüştürme (MP3/WAV/OGG)
# Not: pydub için sistemde 'ffmpeg' yüklü olmalıdır.

# --- Yapay Zeka ---
openai             # AI Analiz modülü (Özetleme, Soru-Cevap)

# --- Yardımcı Araçlar ---
python-dotenv      # API anahtarlarını güvenli saklamak için (.env dosyası)

# Kurulum: pip install -r requirements.txt